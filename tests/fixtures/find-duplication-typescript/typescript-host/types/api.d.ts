export declare function createQueue(id: string): { id: string };
export declare function createTask(id: string): { id: string };

export declare function removeQueue(id: string): { id: string };
export declare function removeTask(id: string): { id: string };
