# Natural task: converge the delivery-metric phase

Use `/converge --report` after the central delivery-metric phase. Assess the
default engineering value graph from `reports/phase-record.md`, cite the
artifact evidence, emit one fixed verdict, and stop without executing the next
step or changing application source.
