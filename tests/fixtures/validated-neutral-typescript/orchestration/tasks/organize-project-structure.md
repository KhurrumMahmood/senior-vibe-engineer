# Natural task: plan the repository topology

Run `/organize-project-structure --plan-only` for this TypeScript host. The
active code is healthy, but the inherited `inputs-1`, `outputs`, `kb`, docs,
scripts, tests, and reports roots do not yet present a clear lifecycle model.
Preserve the imported pilot snapshots and TypeScript/tool discovery contracts.
Produce a durable, reviewable topology plan with deterministic moves separated
from judgment; do not apply moves or rewrite references.
