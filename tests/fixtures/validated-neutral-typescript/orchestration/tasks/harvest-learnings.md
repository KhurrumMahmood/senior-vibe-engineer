# Natural task: harvest delivery-host learnings

Mine this TypeScript host's lint, ADRs, and known issue alongside latent
production standards the small pilot has skipped. Produce the proposal-only
harvest report and machine-readable sidecar. Apply the framework, language,
and domain translation test to every candidate, keep durable source handles,
and do not adopt a standard or edit application code.
