# ADR 0002 — Keep the pilot delivery outcome vocabulary

Partner reconciliation consumes exactly `accepted`, `rejected`, and `retried`
under the `delivery.*` metric namespace. Those words are a pilot contract, not
a general naming standard for unrelated systems.
