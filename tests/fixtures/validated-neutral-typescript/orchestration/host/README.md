# Delivery operations host

A locked TypeScript host for webhook intake, delivery retries, and operational
metrics. The active application lives in `src/`; historical fixtures and run
records still use inherited root names that need a plan before any movement.
