# Natural task: promote the webhook delivery contract

You are working in the TypeScript planning host. An architected plan already
has no P0 decisions and has an explicit decision conformance note. Promote it
to one draft spec. Track `src/api/webhook.ts` and `src/worker/delivery.ts` as
code roots, transcribe the plan's behavior-preserving checks, and preserve its
one P1 operational-dashboard deferral in Exceptions.

Do not implement the contract, edit source code, or create another ADR.
