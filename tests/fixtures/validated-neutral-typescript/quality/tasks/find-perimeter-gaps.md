Audit the TypeScript quality host after adding its worker and ingress modules.
We installed only the perimeter auditor, so report honestly whether that
selected detector fleet covers the significant `src/` TypeScript cell. Do not
add or relabel a detector to make the matrix look clean.
