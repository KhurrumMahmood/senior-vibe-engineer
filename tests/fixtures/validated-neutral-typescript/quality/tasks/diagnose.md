The TypeScript quality host's queue adapter reports that retry scheduling now
fails its direct contract check. Establish a trustworthy diagnosis before any
source change. Preserve the TypeScript source tree and use the existing
`diagnostics/retry-contract.ts` reproducer rather than guessing a fix.
