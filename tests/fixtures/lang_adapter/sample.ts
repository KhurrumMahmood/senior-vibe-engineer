// Fixture for lang_adapter tests — column-0 declarations only.
function loadInvoice() {
  return 1;
}

const fetchShipment = (id) => {
  return id;
};

class CustomerWidget {
  render() {
    return null;
  }
}

window.bootInventory = function () {
  return 0;
};
