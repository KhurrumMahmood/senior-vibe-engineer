# Django binding

Treat Django app layout, settings discovery, management-command discovery, and
static asset conventions as hard constraints when adapting the ideal logical
topology to a host repository.
