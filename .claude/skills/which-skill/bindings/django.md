# Django binding

The canonical cross-stack routing example is a Django-bound high scorer on a
TypeScript host. It must appear under `excluded_inactive[]` with every
activation reason and must never be silently omitted.
