# Django binding

Prefer a Django test over a lexical lint for URL resolution, view-callable,
authentication, CSRF, model, admin, or management-command contracts. Keep the
test focused on the observable contract that the framework owns.
