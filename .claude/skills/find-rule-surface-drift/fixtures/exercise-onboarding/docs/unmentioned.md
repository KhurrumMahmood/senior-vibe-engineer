# unmentioned.md

Stand-in doc that is registered in CLAUDE.md and exists on disk, but
is NOT linked from ONBOARDING.md. The fixture's expected outcome is
that this file triggers `dormant_in_onboarding` exactly once.
