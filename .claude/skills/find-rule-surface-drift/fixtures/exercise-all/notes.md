# Notes

A miscellaneous artifact under the fixture root that mentions
`present.md` and `huge.md` so neither fires `unreferenced_doc`. This file
is NOT under `docs/` and so does not itself participate in any band.
