# Django binding

For workflow-registry cleanup, run the host's site-workflow Django tests. When
a development server is available, also run `testing/test_site_pages.py` to
preserve the rendered workflow across desktop and mobile paths.
