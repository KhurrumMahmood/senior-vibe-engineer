# Lessons (synthetic for extraction-truth-set)

## Hydration parse drift across framework versions

Rule: SSR hydration shapes drift per major version.
Why: cross-version drift broke an early prototype.
How: detect framework + version before parsing the hydration blob.
