"""Half-done scratch prototype. No ledger entry; should surface as a
file-orphan candidate to extract-existing-ideas.

Synthetic fixture file — never imported by production code.
"""
from __future__ import annotations


def maybe_useful_thing() -> str:
    return "candidate"
