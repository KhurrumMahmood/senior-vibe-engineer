# Pending threads — 2026-06-12 session (consolidation board)

Rule applied: every open thread lands here with a route. Status values:
IN-FLIGHT (agent running), QUEUED (decided, needs dispatch), DESIGN
(needs an experiment/plan written), OPERATOR (only the human can act),
PARKED (deliberate).

## In-flight

| Thread | Route |
|---|---|
| parents[4] de-baking sweep (11 hard leaks + 1 default; which-cleanup pattern) | implementer agent running → review, commit |
| /repair-skill text-alone adversarial pass (validation-plan obligation) | reviewer agent running → fix-or-ledger findings |

## Queued (decided, cheap to dispatch)

| Thread | Route |
|---|---|
| Remaining class sweeps: missing declared-verdict block; ceremony stages (output consumed by nothing); read-only-dispatch+write contracts | one detector+fix batch per class, repair-skill Stage-8 style |
| Next full repair loops: /fix-workflow, /prevent-regression | /repair-skill; prevent-regression is Bucket A — verify via skill-comply, the first machine-checked repair |
| Class-lift rollout to /diagnose Phase 7 + /fix-workflow closeout | small spec'd edits via the repair loop |
| skill-comply: fix inherited wrong-name doc/code discrepancy; then recall-axis fixture; then first Bucket-B oracle (proposer completeness) | direct small fix; then incremental scorers per 02b |
| success-game/initial-chat.md harvest (early agent-governance design trace) | one sweep agent, ledger intakes |
| meta-failure-audit-kit port (adversarial lane for change specs / ADRs) | port agent, parity-first like skill-comply |

## Design (needs a written experiment before execution)

| Thread | Route |
|---|---|
| Memory pilot: stage-tag refactor-subsystem R1–R44, injection hook at phase boundaries, measure decision-change vs no-injection baseline | experiment write-up; instruments exist (lift probes) |
| Dreaming / simulation-gated promotion (3–5 variation dream pack incl. must-not-fire case) | design after memory pilot baseline exists; ledger: simulation-gated-promotion |
| Abstraction-lens experiments (abstract-up / common / different) | designed experiment; baseline = reports/summary-pyramid + host-a audit pair |
| role-agents pilot: one veto/curation role over real repair residuals | pilot protocol exists in host-a research dir; needs adaptation + memory-injection v1 |
| Ecosystem-review actions: 2,700-LOC clone dedup, detect:guard 27:1, 02c package shape | big — route through /scope-feature or fold into shareable-core-reorganization plan |

## Operator (your call)

| Thread | Note |
|---|---|
| ~/Projects/server/ holds live AWS .rdp + Worker.pem | move/delete |
| es2 / es3 archive-or-delete | histories fully contained here; verified |
| which-shape boost-vocabulary curation list (6 restored tokens) | review in shapes.yml comments |
| content-pipeline: card items 1–3 + add_lettering disposition | agent-triage needs your review (card-93 precedent) |
| claude-skill-ecosystem/backlog.md (not git-initialized) | init it, or harvest into ledger and retire |

## Parked (deliberate)

Borrowed Crown slice + visual-corpus queue packet + COMPARATIVE_AUDIT
disposition (content-pipeline; separate session). /tmp artifacts
(refactor-skill-old/new, skill-repairs-old, host-a-dogfood) kept until
sweeps no longer need them as fixtures.

## Closed this session (for the record)

refactor-subsystem repair + round-2 + lift + dogfood; diagnose /
which-shape / scope-feature repairs + lift; which-shape Path A;
plan-skill retrofit; /repair-skill created; skill-comply ported;
which-cleanup de-baked; P4 belief-re-arm fused; class-lift gate
installed; forgotten-sweep + triage; es2 question retired; ~15 ledger
intakes/lessons banked.
