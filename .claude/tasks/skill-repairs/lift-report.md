# Lift report — diagnose / scope-feature / which-shape repairs

Protocol: lift-protocol.md (criteria locked before results). Model:
haiku for all cells (the tier where lift concentrates, per the
refactor-subsystem grid). n=1/cell.

| Probe | Old | New | Predicted |
|---|---|---|---|
| P-D root-cause acceptance | **PARTIAL-BAR, fabricated grounding** — demanded H2–H4 probing but attributed the mandate to text that doesn't contain it ("Phase 5 mandates...", "the skill prohibits declaring root cause until all ranked hypotheses have been tested" — neither exists in the old skill); no transcript/proof standard for root-cause.md ("one sentence per the skill") | **FULL-BAR, text-grounded** — quoted the real C2 line ("Consistent evidence is not discriminating evidence"), required a ruling observation per hypothesis, cause-toggle confirmation for H1, residual uncertainty recorded | old=ACCEPTS-NARRATIVE (wrong), new=FULL-BAR (right) |
| P-S advancing status | **FORMAT-ONLY** — straight to Stage 5 status bump + audit; stop conditions purely mechanical (file perms, audit exit) | **FULL-BAR** — Stage 4.5 executed: three borderline changes adjudicated from §1 without asking the user, tighten-on-ambiguity, verdicts consumed by the Stage 6 summary | confirmed both |
| P-W cue-free prompt | **HEDGED** — led with "Recommended shape: Bug Fix" then heavy caveats + clarifying questions | **GATED** — cited the low-confidence rule, refused single-shape presentation, pasted both mandated script lines verbatim, top-3 alternatives + one discriminating question | confirmed both (old in predicted range) |

## Verdict

1. **Full lift at every site, zero regressions** — same shape as the
   refactor-subsystem grid: in-phase mandates move weak-tier behavior
   to the top score.
2. **New finding (P-D old): right-ish behavior on fabricated
   grounding.** The old-condition executor partially invented the
   correct discipline — and attributed it to skill text that doesn't
   exist. This is the benign twin of the P1 fabrication mode: the
   executor's prior happened to point the right way, so the
   fabrication looks like compliance. It is brittle (a different
   prior fabricates the opposite) and it inflates old-condition
   scores: **probe grids underestimate repair lift whenever the
   executor's training prior overlaps the repair's content.** The
   repair's real value at such sites is grounding — the new executor
   cites the actual mandate, so the behavior survives executors whose
   priors point elsewhere.
3. P-W old was better than the review predicted (hedged, not
   confident-arbitrary) — consistent with probe-adherence-is-an-
   upper-bound; the review's misroute evidence came from the script
   layer, which the repair fixed in the scorer itself (probe B:
   Concept Rename now wins at the script level, no prose needed).

Bounds: n=1/cell, single tier, spec-author-authored scenarios,
clean-context upper bound.
