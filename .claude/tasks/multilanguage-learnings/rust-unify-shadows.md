# Rust unify-shadows learning and economics packet

## Outcome and trust boundary

The Rust branch now turns one complete, confirmed `RSD-*` static lead into the
same three-file read-only handoff as the preserved proposal consumers:
`proposal.md`, `evidence.json`, and `scope.json`. The operator supplies the
proposal shape explicitly because the accepted Rust detector does not infer
one. The artifact records that provenance and still requires separate human
approval before `/fix-workflow`; neither shape input nor static evidence
authorizes source consolidation.

The consumer validates the exact accepted semantic fact pack, its canonical
hash, the detector's matching fact-pack/hash/limits lineage, the full
source/configuration snapshot, every source hash, stable-LSP state, clean
compiler state, Cargo 2024 targets, top-level function/caller citations, and
the generated matrix rows. It atomically writes only beneath
`reports/unify-shadows/` and preserves a previous valid proposal when new input
is invalid or stale.

The reused Cargo 2024 fixture proves a useful positive, a clean/no-finding
abstain, an impl/trait-style boundary must-not-fire, partial and failed input,
stale-source rejection without replacement, unsafe output rejection, exact
source preservation, copied-layout execution under `python -I -S`, all four
read-only shapes, and locked/offline metadata/check/test/Clippy/rustfmt/smoke
before and after the positive proposal. Its existing unsafe and macro cases
remain outside confirmed leads.

## Explicit non-claims

No output establishes behavioral equivalence, safe refactoring, canonical
ownership, complete caller reachability, or source-change approval. Traits,
blanket impls, generics/monomorphization, macros/procedural macros, unselected
cfg/features/targets, unsafe/FFI, runtime behavior/reachability, side effects,
ordering/concurrency, external API ownership/compatibility, and semver
compatibility remain unresolved. Native checks validate only the selected
locked build and do not close those gaps.

## Copied closure and economics

The independently runnable closure is `unify-shadows` plus the already
accepted `analysis.json`, matching semantic fact pack, and adjacent capability
matrix. The 648-line, 29,889-byte adapter is stdlib-only and contains no Cargo,
LSP, map-runtime, or detector implementation. It does not need a sibling skill
at proposal time.

Reusing the accepted 622-line fact provider and 350-line duplication detector
avoids embedding 972 provider lines in this consumer. A duplicated closure
would be 1,620 source lines (`648 + 622 + 350`); the artifact-consuming closure
is 648, a 972-line / 60.00% provider-source saving. The dynamic test requires
at least 40% saving. The already accepted bounded fixture remains 27 files and
284 physical lines; this lane adds no duplicate fixture.

## Root integration

Cherry-pick this lane after the accepted Rust semantic-family commit (present
in base `8bf6a0c`). Root should then add only its owned Rust routing/SKILL and
coverage/publication changes, teach the installed command to pass the accepted
analysis, fact pack, finding ID, and explicit shape, and run focused plus
preserved-language/router gates. Do not add `map-subsystem` as a runtime
companion for this consumer: copied-layout proof intentionally consumes frozen
accepted artifacts without importing producer code.
