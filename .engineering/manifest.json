{
  "version": 1,
  "skills": {
    "default": "active",
    "inactive": {
      "find-frontend-duplication": "This toolkit repo has no application frontend (only skill test fixtures); no component/template surface to dedupe.",
      "find-frontend-contract-drift": "This toolkit repo has no application frontend or JS boot contract (only skill test fixtures).",
      "find-route-sprawl": "This toolkit repo has no HTTP route surface (stdlib CLI toolkit; only skill route fixtures)."
    }
  }
}
