{
  "version": 1,
  "skills": {
    "default": "active",
    "inactive": {
      "find-frontend-duplication": "ES2 has no application frontend (only skill test fixtures); no component/template surface to dedupe.",
      "find-frontend-contract-drift": "ES2 has no application frontend or JS boot contract (only skill test fixtures).",
      "find-route-sprawl": "ES2 has no HTTP route surface (stdlib CLI toolkit; only skill route fixtures)."
    }
  }
}
